# Release Notes — 2026-05-07

> Release notes for the 2026-05-07 cohort, demonstrating the format established by **ADR-070** (T-0577).
>
> Earlier commits in this release (T-0565, T-0576) landed before the release-notes contract existed and are documented retroactively. Later commits (T-0577 onwards) were authored against the template at merge time. The CI gate at `.github/workflows/release-notes-check.yml` enforces this on every PR going forward — note that today's commits ff-merged directly to `main` from worktree branches without going through PR review, so the gate ran on none of them and the table was hand-curated as commits landed.

## Operator impact

| Change | Bucket | Reindex? | Re-ingest? | Selectivity | Action required |
|---|---|---|---|---|---|
| `fix(search): hyphen crash + bullet items invisible to FTS` (`232cd8f`, T-0565) | `reingest_selective` | no | selective | `pipeline LIKE 'text.short@v1' AND graph_json IS NOT NULL AND graph_json NOT LIKE '%"node_type":"summary"%'` | `ctxt upgrade run --filter pipeline=text.short@v1` |
| `fix(config): per-binary config files under shared contexthelp/ namespace` (`493e469`, T-0576) | `none` | no | no | n/a | manually rename `~/.config/contexthelp/config.yaml` → `~/.config/contexthelp/dpkms.yaml` (one-shot, not daemon-managed) |
| `docs(adr-070): pipeline & index versioning, upgrade taxonomy, operator consent` (`b1dcb53`, T-0577) | `none` | no | no | n/a | none (ADR + scaffolding only) |
| `docs(adr-071): embedding index versioning and dual-write migration` (`b1dcb53`, T-0578) | `none` | no | no | n/a | none (ADR only — implementation gated on preconditions) |
| `fix(pipeline): route structured markdown to text.long regardless of length` (`9d356a5`, T-0575) | `reingest_selective` | no | selective | `pipeline LIKE 'text.short@v%' AND <markdown structure detected in raw_content>` | `ctxt upgrade run --filter pipeline=text.short@v0` *(after T-0581)* |
| `feat(find): distinguish "no candidates" from "all-below-threshold"` (`fad9d56`, T-0574) | `none` | no | no | n/a | none (additive `diagnostics` block on search response; pre-existing consumers see byte-identical results) |
| `fix(analyze): fail loudly on unrouted pipelines + honor --wait` (`61ff102`, T-0562) | `none` | no | no | n/a | none — historical jobs that were silently dropped (pre-fix) cannot be recovered. New jobs that would have been silently dropped now return 422 `PIPELINE_NOT_FOUND` and `--wait` surfaces non-zero exit on missing job |
| `feat(health): /healthz endpoint + ctxt status + dpkms healthcheck` (`718e8bd`, T-0564) | `none` | no | no | n/a | none (additive endpoint + new CLI commands) |
| `feat(cli): ctxt capture command + flag rename + hint plumbing` (`297b2d7`, T-0566..T-0573) | `none` | no | no | n/a | none — additive command + additive `user_hints` column on jobs (migration 028, NULL-safe for existing rows) |
| `feat(install): dpkms install --launchd / --systemd subcommand` (`bfae504`, T-0563) | `none` | no | no | n/a | optional: `dpkms install --launchd` (macOS) or `--systemd` (linux) to register dpkms as a user service |
| `feat(storage,pipeline): index_signatures table + pipeline_version stamping` (`1d0005d`, T-0579) | `reindex_auto` | yes | no | n/a | automatic (first-boot stamp; no rebuild this release) |
| `feat(upgrade): operator-facing CLI surface for ADR-070 buckets` (`23b0978`, T-0580) | `none` | no | no | n/a | none (additive CLI commands + extends `/healthz` envelope) |
| `feat(upgrade): selective re-ingest engine + staleness warning` (`0f26b58`, T-0581) | `reingest_selective` | no | selective | `pipeline=text.short@v0` (and any other family the registry has bumped past `@v0`) | `ctxt upgrade plan && ctxt upgrade run --filter pipeline=text.short@v0` |
| `feat(capture): plumb --profile + --note end-to-end` (`42e362e`, T-0588) | `none` | no | no | n/a | none — additive `user_profile` + `user_note` columns on jobs (sqlite migration 031, postgres idempotent helper); `ctxt capture --profile <name> --note <text>` now plumbed end-to-end (mirrors T-0190 mentions / T-0573 hints pattern) |
| `fix(capture): address 3 copilot review findings on PR #32` (`6d2bccf`, T-0588 follow-up) | `none` | no | no | n/a | none — gosec ALTER unroll (postgres), smart-quote typo fix in two comments, new `handlers_inbox_test.go` coverage. Bonus: sqlite migration 031 converted from raw ALTER SQL to idempotent Go fn (`migrate031JobsUserProfileNote`, mirrors `migrate014RemindAt`) so re-Init on a partially-migrated DB doesn't trip duplicate-column when the WAL hasn't checkpointed |
| `refactor(storage): port sqlite migrations 027 + 028 to idempotent Go fns` (`28b0b5d`, T-0186 partial) | `none` | no | no | n/a | none — extracts a shared `addJobsColumnIfMissing` helper, ports migrations 027 (user_mentions) and 028 (user_hints) from raw ALTER SQL to idempotent Go fns following the 014/022/031 pattern. Defence in depth: re-Init on a partially-migrated DB no longer trips duplicate-column on these two migrations. Note: the broader test-infra cross-handle visibility issue (TestList / TestShow / TestEdit failing under WAL when one driver handle is open during another's Init) is **not** fixed by this commit; see T-0186 for the systemic-fix investigation |

---

## Changes by bucket

### `reindex_auto` changes

#### `feat(storage,pipeline): index_signatures table + pipeline_version stamping` — T-0579

- **What changed**: established the signature substrate (Part A) + pipeline_version
  string convention (Part B) for bucket-1 detection. First daemon startup
  computes and stores the signature; future signature changes will trigger
  the reindex_auto flow (T-0581 ships the actual rebuild worker).
- **Signature bump**: n/a (first install — no prior signature).
- **Estimated rebuild wall-clock**: 0s (no rebuild this release; storage
  scaffolding only).

### `reingest_selective` changes

#### `feat(upgrade): selective re-ingest engine + staleness warning` — T-0581

- **What changed**: ships the runtime behind the `ctxt upgrade run` stub from
  T-0580. New `internal/upgrade/{selector,worker}.go` parses
  `--filter pipeline=<name>@vN` (or `--where <pred>`) into a SQL predicate,
  iterates the matching set deterministically, and re-runs the registry's
  current pipeline against each object's `RawContent` via the new
  `Service.ReanalyzeObject`. The upgrade-state machine from T-0580 ticks
  through Start → Tick(N) → Complete so `ctxt upgrade status` (and the CLI
  banner) reflect live progress. `ctxt upgrade plan` is now predicate-driven
  too: iterates the registry, counts rows per family below current version,
  emits the (from→to) table with the exact `--filter` invocation per row.
- **Selector predicate**: `pipeline=text.short@v0` (the canonical example —
  any object whose stored `pipeline` stamp parses to a version older than
  the registry's currently-installed version qualifies). The `--where`
  escape hatch supports complex selectivity like
  `where:graph_json IS NULL AND pipeline LIKE 'text.short%'`.
- **Versioned pipeline?**: **Yes**. The `@vN` convention is now load-bearing
  — bare `--filter pipeline=text.short` is rejected at parse time so an
  operator can't accidentally re-ingest every version of a family.
- **Operator-Impact reason**: this is the FIRST release where
  `ctxt upgrade run` actually does work. T-0565 (search hyphen crash + bullet
  items invisible to FTS) shipped its `ProjectIndex` fix before the upgrade
  taxonomy existed; the operator-facing path to re-ingest those affected
  objects has been the manual `ctxt analyze --reanalyze <id>` loop. Once
  T-0581 lands, the supported flow is `ctxt upgrade plan` to discover what's
  pending, then `ctxt upgrade run --filter pipeline=text.short@v0` to clear
  it. The first time an operator runs this on a corpus that pre-dates the
  registry bumping `text.short` past `@v0`, the worker executes the
  pending re-ingest backlog from T-0565 onwards.
- **Estimated wall-clock**: ~50ms/object × pending count (matches T-0565's
  per-object cost since this re-runs the same pipeline against the same
  RawContent).
- **Estimated LLM cost**: **$0** for projection-only re-ingests — the worker
  surfaces a per-object cost field, but the current pipelines (`text.short`,
  `text.long`) don't call LLMs during re-analysis. Future LLM-driven
  pipelines will populate the cost meter; the `--budget USD` flag is wired
  to abort cleanly when the cumulative cost exceeds the cap.
- **Soft staleness signal**: search responses now carry
  `diagnostics.staleness_warning.{count,reason}` when one or more candidates
  are stamped with an older version than the registry's current. Search
  still returns the (possibly stale) results — the warning prompts the
  operator toward `ctxt upgrade plan`. `ctxt find` renders this as a
  `Note:` line under the results table.
- **Bus topics added** (ADR-070 §6, 4-segment past-tense):
  `dpkms.upgrade.plan.computed`,
  `dpkms.upgrade.reingest.{started,progressed,completed,failed}`.

#### `fix(search): hyphen crash + bullet items invisible to FTS` — T-0565

- **What changed**: `internal/projection/projection.go` `ProjectIndex` falls back to flat fields when the graph branch yields no Summary/Section content. Plus `internal/search/safe_fts.go` adds query sanitisation against FTS5 operator-injection (hyphens, colons, etc.).
- **Selector predicate**: `pipeline LIKE 'text.short@v1' AND graph_json IS NOT NULL AND graph_json NOT LIKE '%"node_type":"summary"%'`
  - Translated: objects routed to the (now-versioned) `text.short@v1` pipeline whose graph carries Tag/EntityMention nodes but no Summary node. These are the documents whose body lived only in `TextContent` and was therefore invisible to FTS pre-fix.
- **Versioned pipeline?**: **No** (retroactively — the versioning convention is being adopted in this same release per ADR-070; legacy objects are stamped `@v0` and the post-fix `text.short` becomes `@v1`). For future fixes of this shape, the answer must be **yes**: gate behind a new `@vN`.
- **Estimated wall-clock**: ~50ms/object × affected count. For a typical corpus (<10K objects), most are not affected; running `ctxt upgrade plan` first is the responsible move.
- **Estimated LLM cost**: **$0** — this is a projection-only change. No new embeddings, no LLM inference. The re-ingest re-runs `entity_extractor` over `RawContent` and rewrites `projected_fts_body` from the corrected `ProjectIndex`.

**Verification (ben suite)**: not retroactively measured. Going forward, this kind of fix lands with a `suites/recall-text-short.ben.yaml` row showing pre/post recall on the affected subset. The test suite added in commit `232cd8f` proves correctness on a representative fixture (`TestFindByText_GraphWithoutSummaryIndexesListItems`), but is unit-level rather than corpus-level.

#### `fix(pipeline): route structured markdown to text.long regardless of length` — T-0575

- **What changed**: new `HasMarkdownStructure(content)` predicate (>=1 heading or >=5 bullet items) ORs into `text.long`'s `ContentTest`; `Def.Priority` field added so `text.long` wins over `text.short` when both content-tests match. Markdown bullet-list documents that previously fell through to `text.short` (no `markdown_parser`/`sectioner`) now run the structured pipeline and emit a root Summary node carrying full content.
- **Selector predicate**: existing objects whose graph lacks Summary/Section nodes despite the raw content having markdown structure — the same population T-0565 partially addresses via the projection-fallback. The narrowest predicate is `pipeline LIKE 'text.short@v%' AND <regex match for markdown headings or bullet lists in raw_content>`. T-0581's `--filter pipeline=text.short@v0` is a sufficient (and slightly broader) substitute.
- **Versioned pipeline?**: implicit — no new pipeline name was registered, but the routing decision changed. Future routing changes of this shape should bump a versioned pipeline name (e.g. `text.long@v2`) so the operator can opt-in via `--filter`.
- **Estimated wall-clock**: same as T-0565 (~50ms/object × affected count). The re-ingest reuses the same RawContent and re-runs whichever pipeline the post-fix routing selects.
- **Estimated LLM cost**: **$0** for projection-only re-runs. If the re-routed pipeline (`text.long`) calls more LLM-driven steps than the original (`text.short`), cost may be non-zero per object — surface via the worker's per-object cost meter post-T-0581.

### `none` changes (no operator impact)

- **`493e469` (T-0576)** — config-file rename. Operator must rename `~/.config/contexthelp/config.yaml` → `~/.config/contexthelp/dpkms.yaml` (or `ctxt.yaml` if they were running ctxt-side config), but no daemon-managed re-ingest or reindex.
- **ADR-070** — framework only. No code changes that affect stored knowledge or the index.
- **ADR-071** — design document only. Implementation is gated on three preconditions (ADR-071 §Three preconditions); precondition 1 (ADR-070 shipping) was met today, preconditions 2 (ben recall suite) and 3 (second use case) are pending.
- **`23b0978` (T-0580)** — `ctxt upgrade {status,plan,run}` CLI surface, `/healthz` upgrade envelope, banner middleware. Additive only: existing `/healthz` consumers see byte-identical payloads when no upgrade is in flight (the `upgrade` field is omitempty), and the new CLI subcommands don't touch stored knowledge or the pipeline contract. `run` was a refuse-stub at T-0580 time; T-0581 fills in the body.
- **`fad9d56` (T-0574)** — search-result honesty. `SearchDiagnostics` envelope on hybrid-search responses (`candidate_count`, `below_threshold_count`, `top_below_threshold_score`, `threshold`). Distinguishes "no docs matched any token" from "matches existed but all dropped under threshold" in `--json` output and human renderer. Pre-existing consumers without the field see byte-identical results.
- **`61ff102` (T-0562)** — `ctxt analyze --file <pdf>` was silently dropping jobs (returning a Job ID for a pipeline that didn't actually persist). Now: unrouted types return 422 `PIPELINE_NOT_FOUND`; `--wait` polls and surfaces non-zero exit when the job ID isn't found in the queue within timeout. **Historical jobs lost pre-fix cannot be recovered** — they were never persisted. From this release forward, the silent-drop path is closed.
- **`718e8bd` (T-0564)** — new `/healthz` HTTP endpoint, `ctxt status` and `dpkms healthcheck` CLIs. Additive endpoint, additive commands. Operator can now answer "is dpkms healthy?" without parsing `dpkms ps`.
- **`297b2d7` (T-0566..T-0573)** — `ctxt capture` command + flag rename + `--hint` plumbing. Additive new command + additive `user_hints` JSON column on jobs (migration 028, NULL-safe for existing rows). Mirrors the T-0190 mentions plumbing in design.
- **`bfae504` (T-0563)** — `dpkms install --launchd` (macOS LaunchAgent) and `--systemd` (linux user service) subcommands. Optional install step; the daemon stays alive across crashes/login cycles. No state change.

---

## Upgrade walkthrough

### Step 0 — Back up before upgrading

```bash
ctxt backup create --to ~/ctxt-backups/pre-2026-05-07.tar.gz
```

### Step 1 — Pull / install

```bash
# however you install, e.g.:
git pull && make install
```

### Step 2 — Rename config file (T-0576)

```bash
# If you run dpkms (most common):
mv ~/.config/contexthelp/config.yaml ~/.config/contexthelp/dpkms.yaml

# If you have ctxt-only config (rare):
# mv ~/.config/contexthelp/config.yaml ~/.config/contexthelp/ctxt.yaml
```

### Step 3 — Plan the selective re-ingest (T-0565 + T-0581)

```bash
# Dry-run first to see what's affected:
ctxt upgrade plan
```

T-0581 ships the predicate-driven plan — the output looks like:

```
Upgrade plan:
  reindex_auto:
    objects_fts: signature ok (no rebuild needed)
  reingest_selective:
    text.short@v0 → text.short@v1: 47 objects
      Run: ctxt upgrade run --filter pipeline=text.short@v0
  reingest_all:
    none

Total objects pending: 47
```

If your `text.short` pipeline registration hasn't been bumped past `@v0` (a
fresh corpus, or a corpus that never had T-0565 applied retroactively), the
selective row will be `none`. Run `ctxt upgrade plan` after upgrading to
this release to confirm what your specific corpus needs.

### Step 4 — Run the re-ingest

```bash
ctxt upgrade run --filter pipeline=text.short@v0 --dry-run   # confirm match count
ctxt upgrade run --filter pipeline=text.short@v0             # execute
```

### Step 5 — Verify

```bash
ctxt upgrade status --watch  # tracks Start → Tick → Complete in real time
# or:
ctxt find SageMaker  # the original repro from T-0565 — should now return hits
```

If your search results also include a `Note:` line about objects pending
upgrade, that's the new T-0581 staleness signal — re-run `ctxt upgrade plan`
to see what's still pending and `ctxt upgrade run --filter ...` to clear it.

---

## Verification

ben suite results — not retroactively captured. ADR-070 mandates this for future PRs of this shape.

For T-0565 specifically, verification is via the unit test added in `232cd8f`:

- `TestFindByText_GraphWithoutSummaryIndexesListItems` — pre-fix: 0 hits for "SageMaker" / "VMware" / "Bedrock" against a bullet-list KO. Post-fix: hit on every term.

---

## Notes

- The retroactive labelling here is a one-time exercise to seed the format. From this release forward, every PR fills in the table at PR time, not after the fact.
- ADR-070 and ADR-071 are themselves marked `none` because they're documents. ADR-070's implementation phases (T-0579, T-0580, T-0581) all shipped today and are individually rowed above. ADR-071's implementation tasks (T-0582, T-0583, T-0584, T-0585) are pending — gated on the eval-harness precondition (T-0587 ben adoption) plus a second concrete use case beyond migration.
- The user-facing rename in T-0576 is `none` per ADR-070's strict definition: the daemon does not re-ingest or reindex, the operator does a one-shot file move. We keep it visible here under the table because it *is* operator-affecting work, just not in a way the upgrade taxonomy claims jurisdiction over. Future ADRs may revisit whether the taxonomy should grow a fourth bucket for this shape.

---

## References

- ADR-070 — Pipeline & Index Versioning, Upgrade Taxonomy, Operator Consent
- ADR-071 — Embedding Index Versioning and Dual-Write Migration
- T-0562 — analyze --file silent-drop fix
- T-0563 — dpkms LaunchAgent / systemd install subcommand
- T-0564 — /healthz endpoint + ctxt status + dpkms healthcheck
- T-0565 — search recall fix (hyphen + bullet items)
- T-0566..T-0573 — ctxt capture command + flag rename + hint plumbing
- T-0574 — search result honesty (SearchDiagnostics)
- T-0575 — pipeline routing for structured markdown
- T-0576 — config auto-discovery fix
- T-0577 — release-notes contract
- T-0578 — embedding-index ADR
- T-0579 — index_signatures table + pipeline_version stamping
- T-0580 — `ctxt upgrade {status,plan,run}` CLI surface + state machine
- T-0581 — selective re-ingest engine + staleness warning
- T-0588 — `ctxt capture` `--profile` + `--note` plumbing + handler test coverage
