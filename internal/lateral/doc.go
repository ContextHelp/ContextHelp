// Package lateral implements capture-time lateral discovery: surfacing
// adjacent entities (siblings, profiles, sponsor pages, similar shapes) for
// every capture, gated by active context, materialized as probationary
// entities with TTL until promoted or expired.
package lateral
