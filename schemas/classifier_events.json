{
  "title": "classifier_events",
  "description": "Classifier-pipeline event payloads. Keys are short-form (Object.Action); the runtime topic is `<source>.<category>.<short_form>` per the [Source].[Category].[Object].[Action] standard. Lateral emits these as `ctxt.lateral.<key>`. Object `classification` is the produced thing; Actions describe how it was produced.",
  "type": "object",
  "definitions": {
    "classification.matched_heuristic":  {"type": "object", "properties": {"source_domain": {"type": "string"}, "page_type": {"type": "string"}}},
    "classification.produced_via_llm":   {"type": "object", "properties": {"source_domain": {"type": "string"}, "page_type": {"type": "string"}, "duration_ms": {"type": "integer"}}},
    "classification.hit_recipe_cache":   {"type": "object", "properties": {"source_domain": {"type": "string"}, "page_type": {"type": "string"}}}
  }
}
